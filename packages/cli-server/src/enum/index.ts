export const EMIT_ENUM = {
  PROJECT_OPTIONS_ENUM: "projectOptions"
};

export const ON_EVENT_ENUM = {
  ON_SUCCESS: "onSuccess",
  ON_INIT_PROJECT_ENUM: "onInitProject"
};

export const HTTP_CODE = {
  SUCCESS: 200,
  ERROR: 500
};
